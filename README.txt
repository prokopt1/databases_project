# ReadMe — Prokop Database Project

To execute and create the database, run the “create_db.py” file, then run the “main.py” file to interact with the database.

The purpose of this project is to create a simple, mock database for student enrollments at a university.  In order to interact with the database, run the “main.py” file, and a command line prompt will appear.
